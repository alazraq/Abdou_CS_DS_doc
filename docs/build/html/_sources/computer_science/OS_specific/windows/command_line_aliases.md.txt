# Setting up command line aliases:

- Create a folder named aliases somewhere
- Add a file my_command.bat inside the folder containing the command you want to be executed
- Add the folder aliases to the PATH environment variable
- my_command is now an alias for your command on the terminal
